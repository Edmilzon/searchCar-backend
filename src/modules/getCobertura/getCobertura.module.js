const traerControlar = require('./getCobertura.controller');
const traerServicio = require('./getCobertura.service');

module.exports = {
    controller: traerControlar,
    service: traerServicio,
}